<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Color conversion for console logging -->
    <conversionRule conversionWord="clr" converterClass="org.springframework.boot.logging.logback.ColorConverter" />
    <conversionRule conversionWord="wex" converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter" />
    <conversionRule conversionWord="wEx" converterClass="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter" />

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %clr(%5p) [%thread] %clr(%logger{36}){cyan} : %msg%n%wEx</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Project Specific Loggers -->
    <logger name="com.f1soft.hazelcast" level="DEBUG" />

    <!-- Hazelcast Cluster & Discovery Logging -->
    <logger name="com.hazelcast" level="INFO" />
    <logger name="com.hazelcast.internal.cluster" level="INFO" />
    <logger name="com.hazelcast.nio" level="INFO" />

    <!-- Spring Cache Abstraction Logging (Shows Cache Hits and Misses) -->
    <logger name="org.springframework.cache" level="TRACE" />

    <!-- Hibernate SQL Query Logging -->
    <logger name="org.hibernate.SQL" level="DEBUG" />
    <logger name="org.hibernate.type.descriptor.sql" level="TRACE" />

    <!-- Root Logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>

</configuration>
